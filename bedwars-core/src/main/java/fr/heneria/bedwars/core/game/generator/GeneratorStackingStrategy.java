package fr.heneria.bedwars.core.game.generator;

/** Requested platform behavior when a resource is emitted near existing drops. */
public enum GeneratorStackingStrategy {
  MERGE_NEARBY,
  SEPARATE_ITEMS
}
