package fr.heneria.bedwars.core.arena;

/** Administrative definition status, deliberately separate from future match states. */
public enum ArenaStatus {
  DRAFT,
  READY,
  ENABLED,
  DISABLED,
  INVALID,
  ERROR
}
